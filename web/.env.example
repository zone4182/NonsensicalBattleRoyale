VITE_FUNCTIONS_URL=http://127.0.0.1:54321/functions/v1
# Public VAPID key for Web Push (safe to expose -- pairs with VAPID_PRIVATE_KEY set as
# a Supabase Edge Function secret, never in this file). Generate with:
#   npx web-push generate-vapid-keys
VITE_VAPID_PUBLIC_KEY=
